[package]
name = "marpc-macros"
version = "0.1.0"
edition = "2021"

[features]
server = []
client = []

[lib]
proc-macro = true

[dependencies]
syn = { version = "2.0", features = ["full"] }
quote = "1.0"
proc-macro2 = "1.0"
